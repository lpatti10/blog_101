 {{#each post}}
	<li>
		<a href="#" id="{{_id}}" class="post_title">{{title}}</a>
		{{!-- <h2>{{{ title }}}</h2> --}}
		<p>{{{ content }}}</p>
		<small>{{{ author }}}</small>
		<small>{{{ date }}}</small>
		<a href="#">{{{ tags }}}</a>
	</li>
{{/each}}